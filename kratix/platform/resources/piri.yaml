apiVersion: marketplace.kratix.io/v1alpha1
kind: app
metadata:
  namespace: default
  name: piri
spec:
  appConfig:
    image: syntasso/sample-todo:v0.1.0
    lifecycle: dev
    requiresBucketBackend: false
    requiresDatabaseBackend: true
    servicePort: 80
